# Adobe Target (at.js): merge Optiview into targetPageParams

Paste **`target-page-params.js`** after [Session Intelligence](https://optiview.ai/install) and `at.js` are on the page.

Adjust **`adobe.target.triggerView`** / **`getOffer`** flow for your MPA vs SPA architecture.

See **`docs/integrations/adobe-target.md`**.
