# Eligibility commercial intent coupling

When `check_eligibility` is strongest and `coverage_or_eligibility_uncertainty` is active at **medium readiness**, the runtime should favor **eligibility guidance** — not appointment urgency or booking pressure.

Seeds `commercial_intent` including optional `form_type_counts.eligibility`. Vitest: healthcare eligibility case in `commercialIntentDecisionCoupling.test.ts`.
