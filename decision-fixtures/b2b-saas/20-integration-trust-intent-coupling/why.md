# Integration + trust commercial intent coupling

When `integration_concern` is active and the visitor is validating security/integration depth at **medium readiness**, commercial intent coupling should rank **integration summary / implementation** surfaces ahead of **guided walkthrough** or workshop escalation.

Seeds `commercial_intent` directly (no DOM). Vitest: `commercialIntentDecisionCoupling.test.ts` B2B integration/security/pricing case.
