# Rate/fee research + finance commercial intent coupling

When `view_financing` / calculator activity is strongest and `pricing_uncertainty` is active at **medium readiness**, the runtime should surface **rate and fee clarity** — not application resume or approval language.

Seeds `commercial_intent` directly. Vitest: finance rate/fee and calculator cases in `commercialIntentDecisionCoupling.test.ts`.
